# Frontend Specification - MariaDB Migration

## User Interface & Assets
The frontend is completely decoupled from the database management system. All interactions are handled via the JSON REST API. 

Therefore, **no changes** are required in the Next.js frontend code, page layouts, services, state, or hooks.
