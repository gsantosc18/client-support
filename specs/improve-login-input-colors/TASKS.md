# Frontend

- [ ] Modificar o componente agnóstico `app/src/components/forms/Input.tsx` para remover as classes `dark:bg-slate-900`, `dark:text-white`, `dark:border-slate-700` e `dark:placeholder-slate-500`.
- [ ] Rodar a suíte de testes de unidade e E2E do Cypress para validar a renderização e o funcionamento estável de todos os formulários.
