# Frontend

- [ ] Modificar `<form>` de `app/src/app/login/page.tsx` para adicionar `method="post"`.
- [ ] Modificar `<form>` de `app/src/app/register/page.tsx` para adicionar `method="post"`.
- [ ] Modificar `<form>` de `app/src/app/forgot-password/page.tsx` para adicionar `method="post"`.
- [ ] Modificar `<form>` de `app/src/app/reset-password/page.tsx` para adicionar `method="post"`.
- [ ] Rodar testes unitários e de integração locais para validar se nenhuma regressão ocorreu no envio dos formulários.
