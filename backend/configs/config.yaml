server:
  port: "8080"
database:
  url: "host=localhost user=root password=rootpassword dbname=client_support port=5432 sslmode=disable TimeZone=UTC"
redis:
  url: "localhost:6379"
jwt:
  secret: "my-secret-key-change-in-prod"
